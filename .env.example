# Environment Variables for Production

# Session secret for Express (generate a random string)
SESSION_SECRET=change-this-to-a-random-secret-key

# Node environment
NODE_ENV=production

# Port (optional, defaults to 5000)
PORT=5000
